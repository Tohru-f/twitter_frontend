import React from "react";
import styled from "styled-components";

const TweetBox = styled.div`
  width: 35%;
  color: white;
`;

export const TweetView = () => {
  return <TweetBox>投稿一覧を表示する</TweetBox>;
};
